version: '3.7'

services:

  # API on web:5000
  # or on <host>:5000
  ibpp:
    build:
      context: './'
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    env_file:
      - .env
    depends_on:
      - neo4j
    networks:
      - web-net_main

  # Bolt on neo4j:7687
  # or on <public_host>:7687
  neo4j:
    image: neo4j:latest
    ports:
      - "7474:7474"
      - "7687:7687"
    env_file:
      - .env
    volumes:
      - data:/data
      - config:/conf
    networks:
      - web-net_main

# Data persistence
volumes:
  data:
  config:

networks:
  web-net_main:
    external: true